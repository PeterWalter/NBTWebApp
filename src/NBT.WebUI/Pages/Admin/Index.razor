@page "/admin"
@inject NavigationManager NavigationManager

<PageTitle>Admin Dashboard - NBT Website</PageTitle>

<div style="padding: 24px;">
    <FluentStack Orientation="Orientation.Vertical" Style="gap: 24px;">
        <FluentStack HorizontalAlignment="HorizontalAlignment.SpaceBetween">
            <div>
                <h1>Admin Dashboard</h1>
                <p style="color: var(--neutral-foreground-hint); margin-top: 4px;">
                    Welcome to the NBT Admin Portal
                </p>
            </div>
        </FluentStack>

        <!-- Statistics Cards -->
        <FluentGrid Spacing="3">
            <FluentGridItem xs="12" sm="6" md="3">
                <FluentCard Style="height: 100%;">
                    <h3 style="margin-top: 0;">Content Management</h3>
                    <p style="color: var(--neutral-foreground-hint);">Manage pages and announcements</p>
                    <FluentButton Appearance="Appearance.Accent" OnClick="@(() => NavigationManager.NavigateTo("/admin/content-pages"))">
                        Go
                    </FluentButton>
                </FluentCard>
            </FluentGridItem>

            <FluentGridItem xs="12" sm="6" md="3">
                <FluentCard Style="height: 100%;">
                    <h3 style="margin-top: 0;">Announcements</h3>
                    <p style="color: var(--neutral-foreground-hint);">News and updates</p>
                    <FluentButton Appearance="Appearance.Accent" OnClick="@(() => NavigationManager.NavigateTo("/admin/announcements"))">
                        Manage
                    </FluentButton>
                </FluentCard>
            </FluentGridItem>

            <FluentGridItem xs="12" sm="6" md="3">
                <FluentCard Style="height: 100%;">
                    <h3 style="margin-top: 0;">Inquiries</h3>
                    <p style="color: var(--neutral-foreground-hint);">Customer messages</p>
                    <FluentButton Appearance="Appearance.Accent" OnClick="@(() => NavigationManager.NavigateTo("/admin/inquiries"))">
                        View
                    </FluentButton>
                </FluentCard>
            </FluentGridItem>

            <FluentGridItem xs="12" sm="6" md="3">
                <FluentCard Style="height: 100%;">
                    <h3 style="margin-top: 0;">Resources</h3>
                    <p style="color: var(--neutral-foreground-hint);">File management</p>
                    <FluentButton Appearance="Appearance.Accent" OnClick="@(() => NavigationManager.NavigateTo("/admin/resources"))">
                        Manage
                    </FluentButton>
                </FluentCard>
            </FluentGridItem>
        </FluentGrid>

        <!-- Navigation -->
        <FluentCard>
            <h3>Management Sections</h3>
            <FluentStack Orientation="Orientation.Vertical" Style="gap: 8px; margin-top: 16px;">
                <FluentButton Appearance="Appearance.Outline" Style="width: 100%; justify-content: flex-start;"
                              OnClick="@(() => NavigationManager.NavigateTo("/admin/content-pages"))">
                    📄 Content Pages
                </FluentButton>
                <FluentButton Appearance="Appearance.Outline" Style="width: 100%; justify-content: flex-start;"
                              OnClick="@(() => NavigationManager.NavigateTo("/admin/announcements"))">
                    📢 Announcements
                </FluentButton>
                <FluentButton Appearance="Appearance.Outline" Style="width: 100%; justify-content: flex-start;"
                              OnClick="@(() => NavigationManager.NavigateTo("/admin/inquiries"))">
                    ✉️ Contact Inquiries
                </FluentButton>
                <FluentButton Appearance="Appearance.Outline" Style="width: 100%; justify-content: flex-start;"
                              OnClick="@(() => NavigationManager.NavigateTo("/admin/resources"))">
                    📦 Resources
                </FluentButton>
                <FluentButton Appearance="Appearance.Outline" Style="width: 100%; justify-content: flex-start;"
                              OnClick="@(() => NavigationManager.NavigateTo("/admin/users"))">
                    👥 Users
                </FluentButton>
            </FluentStack>
        </FluentCard>

        <FluentCard>
            <FluentStack Style="gap: 12px;">
                <FluentButton Appearance="Appearance.Lightweight" OnClick="@(() => NavigationManager.NavigateTo("/"))">
                    ← Back to Site
                </FluentButton>
            </FluentStack>
        </FluentCard>
    </FluentStack>
</div>

@code {
    // Admin dashboard - static version
    // TODO: Add statistics from API once admin endpoints are implemented
}
